# Asteria — 프로젝트 규칙

## 코드는 무조건 내가(사용자가) 물리적으로 친다

이 프로젝트에서 프로덕션 코드를 키보드로 치는 사람은 **사용자 본인**이다. 예외 없음.

- Claude는 **설명·설계·진단·검토**까지만 한다. 클래스, 함수, 파일을 **직접 작성·수정·삭제하지 않는다.**
  - "이렇게 바꾸면 된다"까지는 말로 한다. **바꾸는 손은 사용자다.**
  - Edit/Write로 소스(`Source/**`, 헤더, cpp)를 건드리지 않는다.
- 코드를 미리 써 두고 "복붙하세요"라고 내미는 것도 금지다. 스니펫으로 "예시"를 주는 것도 우회다.
  - 필요하면 **무엇을·어디를·왜 바꿀지 말로** 설명하고 멈춘다. 타이핑은 사용자가 한다.

**왜:** 물리적으로 직접 쳐야 패턴이 손에 남는다. AI가 대신 치면 "내가 만들었다"는 착각만 남고
원리를 물으면 답 못 하는 상태가 된다. 능력은 설계 지시가 아니라 직접 친 시간에서 나온다.

**적용 범위:** 이 규칙은 `Source/**`의 C++ 소스에 적용된다.
`.uasset`/`.umap` 같은 에디터 데이터, MCP를 통한 에디터 조작, 빌드/설정 파일은 별도로 지시할 때 판단한다.
