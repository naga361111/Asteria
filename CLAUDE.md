## 출력 규칙
두괄식(주장 먼저)이나 피라미드 구조(결론 → 이유 → 근거) 형태로 답변을 출력. 가장 중요한 정보를 문서 상단에 배치하라. 한 섹션 = 한 주제 + 계층적 리스트 활용.

답변 시 생소한 어휘(일상생활에서 잘 사용하지 않는 특정 분야의 전문적인 표현, 구현된 코드의 함수명, 변수명 등)는 무작정 사용하지 않음. 사용해야 하는 상황이면 무조건 사전에 어휘에 대해 사용자가 이해할 수 있도록 사전 설명을 포함해야 함.

작업 도중에는 어떠한 출력도 허용하지 않음.
모든 출력/답변은 작업 및 주어진 행동이 전부 완료된 후 답변할 내용을 핵심만 정리해서 사용자에게 전달해야 함.

기본적으로 코드 및 파일 수정 등의 작업은 자동으로 수행하지 않는다.
사용자가 명시적으로 구현하라고 요청할 때만 예외적으로 코드를 작성한다.

## 프로젝트: Asteria

- 코옵 어드벤처러 길드 홀 시뮬 (Unreal Engine, C++). 스코프 A형 확정, 던전은 추상·회색박스 코어부터.
- **핵심 불변조건 — 서버(호스트) 권위 → 클라이언트 동기화.** 경계·계약 검토 시 항상 먼저: (1) 누가 상태를 **소유**하는가, (2) 복제 **방향**은 어디인가, (3) 클라 입력은 어디서 **검증**되는가.
- 소스는 **기능별 폴더로 그룹화**. 모듈 루트(`Source/Asteria`) flat 배치 금지.


## 구현 TodoList